FROM python:3.11-slim

WORKDIR /app

# Install psycopg2
RUN pip install --no-cache-dir psycopg2-binary

# Copy migration script
COPY migrate_cloud.py /app/migrate.py

# Run migration
CMD ["python", "/app/migrate.py"]
